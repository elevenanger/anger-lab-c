/*
 * @author 刘昂林 
 * @date 2022/8/30
 * @brief 格式化浮点数
 */
#include <stdio.h>

int main(void) {
    float f = 34.32323f;
    /* 指数形式表示 字段宽度八位 左对齐 小数点后保留一位小数 */
    printf("|%-8.1e|", f);
    /* 指数形式表示 字段宽度十位 右对齐 小数点后保留六位小数 */
    printf("|%10.6e|", f);
    /* 十进制表示 字段宽度八位 左对齐 小数点后保留三位小数 */
    printf("|%-8.3f|", f);
    /* 十进制表示 字段宽度六位 右对齐 小数点后不保留小数 */
    printf("|%6.0f|", f);

    return 0;
}